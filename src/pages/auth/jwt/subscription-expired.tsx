import { JwtSubscriptionExpiredView } from 'src/auth/view/jwt/jwt-subscription-expired-view';

// ----------------------------------------------------------------------

export default function JwtSubscriptionExpiredPage() {
  return <JwtSubscriptionExpiredView />;
}
